# ─── Required ────────────────────────────────────────────────────────────────

# Discord bot token from https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your_bot_token_here

# Redis connection string
REDIS_URL=redis://localhost:6379

# ─── Optional — Database ─────────────────────────────────────────────────────

# PostgreSQL connection string. Enables audit log, config persistence, and troubleshooting guides.
# DATABASE_URL=postgres://user:password@localhost:5432/alertbot

# ─── Optional — Discord ──────────────────────────────────────────────────────

# Guild ID for instant slash command registration (recommended for development).
# Without it, global commands take up to 1 hour to propagate.
# DISCORD_GUILD_ID=your_server_id

# Fallback Discord channel ID when the payload does not include one.
# DEFAULT_CHANNEL_ID=your_channel_id

# Set to "true" to enable the Message Content privileged intent.
# Needed only for in-thread text collection in /add-guide.
# DISCORD_USE_MESSAGE_INTENTS=true

# ─── Optional — API Auth ─────────────────────────────────────────────────────

# Bearer token for API endpoint authentication.
# AUTH_TOKEN=your_secret_token

# ─── Optional — AWS / SQS ────────────────────────────────────────────────────

# Full SQS queue URL. When set, the bot polls this queue for SNS notifications.
# SQS_ALERT_QUEUE_URL=https://sqs.eu-west-1.amazonaws.com/123456789012/alert-queue

# AWS region of the SQS queue (only needed when different from the bot's region).
# SQS_ALERT_QUEUE_REGION=eu-west-1

# Default AWS region for SDK credential chain.
# AWS_REGION=eu-west-1

# ─── Optional — General ──────────────────────────────────────────────────────

# Override the default alert routing config file path.
# ALERT_CONFIG_PATH=config/alerts.json

# Audit log retention. Examples: 90d, 30d, 365d, or seconds (e.g. 7776000).
# AUDIT_LOG_TTL=90d

# HTTP server port.
PORT=4000

# Pino log level: trace | debug | info | warn | error | fatal
LOG_LEVEL=info

# Set to "production" for structured JSON logs; any other value uses pino-pretty.
NODE_ENV=development
